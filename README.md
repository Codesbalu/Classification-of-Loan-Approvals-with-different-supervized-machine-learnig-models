Using free source bank customer data which consists of 4K+ rows with 9 features(columns)
Using matplot library visaulized the data and find the similarities and relation associated with the data
Removing the outliers in the data and standardized it.
applyed different supervised _machine learinig models like logisticRegression,SVM,KNN,DescionTree,ensemble methods and NavieBayes GaussianNB.
Using different metrics from sikitlearn library got their predictions score
using matplot library and seaborn created a visulaition of the confusion matrix for different model predictions and also the roc_auc_curve of different models
